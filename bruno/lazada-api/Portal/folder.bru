meta {
  name: Portal
}

docs {
  Endpoint สำหรับพนักงานของ partner ใช้ผ่าน portal admin — ต้อง auth ด้วย portal admin
  Bearer token หรือ X-api-key (`get_portal_admin_from_request` ใน `util/portal_auth.py`)
}
